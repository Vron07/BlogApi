This API project invloves the following functionality:

a) Authentication via EMAIL verification, with JWT token.
b) Login with access code(Swagger UI is used).
c) Reset Email, password, forgot password.
d) User can post a blog,put comment to the blog, reply on the same comment

